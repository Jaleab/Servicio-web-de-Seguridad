g++ /codigo/registroLogin.cpp -o /cgi-bin/registroLogin.cgi
chmod +x /cgi-bin/registroLogin.cgi


En /etc/httpd/conf/ esta la configuracion del Apache

en /var/www estan los archivos del contenido del web server

En la carpeta codigo esta el codigo del profe modificado (.c) y el original (.bak) + la vista de loginRegistro.cpp
